<%= form_with(model: blog_blog, local: true) do |form| %>
  <% if blog_blog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(blog_blog.errors.count, "error") %> prohibited this blog_blog from being saved:</h2>

      <ul>
      <% blog_blog.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
