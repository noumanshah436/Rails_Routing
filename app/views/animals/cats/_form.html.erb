<%= form_with(model: animals_cat, local: true) do |form| %>
  <% if animals_cat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(animals_cat.errors.count, "error") %> prohibited this animals_cat from being saved:</h2>

      <ul>
      <% animals_cat.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :age %>
    <%= form.number_field :age %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
